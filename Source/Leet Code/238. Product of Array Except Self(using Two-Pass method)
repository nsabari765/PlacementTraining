public class HelloWorld
{
    public static void Main(string[] args)
    {
        int[] nums = { 1, 2 };
        Console.WriteLine("Product of Array Except Self = " + string.Join(" ", ProductExceptSelf(nums)));
    }

    public static int[] ProductExceptSelf(int[] nums)
    {
        int left = 1;
        int right = 1;
        int size = nums.Length;
        int[] answer = new int[size];

        //Multiply left of i
        for (int i = 0; i < size; i++)
        {
            an
			swer[i] = left;
            left *= nums[i];
        }

        //Multiply right of i and multiply ans with existing number
        for (int i = size - 1; i >= 0; i--)
        {
            answer[i] = right * answer[i];
            right *= nums[i];
        }

        return answer;
    }
}


ref - https://algo.monster/liteproblems/238#code